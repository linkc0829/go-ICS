workspace:
    path: /github.com/linkc0829/go-ics

clone:
    git:
        image: plugins/git
        depth: 50
        tags: true

pipeline:
    testing:
        image: golang:1.15
        environment:
            from_secret: env
        commands:
            - go test -v .
